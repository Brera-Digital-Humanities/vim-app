# File che non devono finire dentro l'image (build context più snello)
.env
.env.local
*.log
.DS_Store
.git/
.gitignore
INSTALL.md
config/config.json
config/*.example
# La cartella images/ (icone PWA hi-res) può essere pesante: include
# solo se serve nel runtime di Enketo.
